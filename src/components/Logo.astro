---
// The header lockup. `src` stays relative, exactly as the export emitted it: with
// build.format 'file' every page lives at the site root, so it resolves to /assets/...
// The footer uses a plain <img> at a different size and alt text — not this component.
---
<img alt="ClairoCare" src="assets/logo-lockup-navy.png" style="height: 34px; width: auto; object-fit: contain; display: block;">
